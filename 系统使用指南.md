# 电力负荷预测与智能分析系统

基于 PyTorch LSTM 模型与百度千帆大模型的智能电网负荷预测解决方案

## 系统功能

### 任务一：负荷数据智能预处理
- 自动读取历史负荷时间序列数据（2021-2024年）
- 缺失值填充：基于时间的线性插值
- 异常值剔除：同期Z-Score智能识别
- 数据标准化：MinMax标准化处理
- 特征工程：时间周期编码（小时、星期）

### 任务二：短期负荷AI预测
- 基于 PyTorch LSTM 深度学习模型
- 实现未来24小时/72小时区域负荷预测
- 输出预测数值、预测曲线与95%置信区间
- 支持调度运行参考决策

### 任务三：负荷异常智能识别
- 自动识别负荷突增、突降异常状态
- 检测持续偏高、持续偏低趋势
- 分析与历史趋势偏离过大的情况
- 标注异常时间、异常幅度与风险等级（红/橙/黄）

### 任务四：预测简报自动生成
- 依据预测结果与异常识别情况
- 通过百度千帆大模型生成专业简报
- 包含关键结论、风险提示、运行建议
- 符合电网调度术语规范

## 技术架构

- **前端**: Bootstrap 5 + Chart.js + Marked.js
- **后端**: Django 2.2
- **深度学习**: PyTorch LSTM模型
- **AI大模型**: 百度千帆 ERNIE-4.0
- **数据处理**: Pandas + NumPy + Scikit-learn

## 安装与配置

### 1. 系统要求
- Python 3.8+
- Windows/Linux/MacOS

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. 配置百度千帆API密钥
有两种方式配置API密钥：

**方式一：环境变量**
```bash
# Windows
set QIANFAN_API_KEY=your_api_key_here

# Linux/MacOS
export QIANFAN_API_KEY=your_api_key_here
```

**方式二：直接配置**
编辑 `generate_report.py` 文件第102行：
```python
api_key = os.getenv("QIANFAN_API_KEY", "your_real_api_key_here")
```

获取API密钥步骤：
1. 访问百度千帆大模型平台：https://qianfan.cloud.baidu.com/
2. 注册并创建应用
3. 获取API Key（格式：bce-v3/xxx）

### 4. 数据准备
确保数据文件 `powerdemand_5min_2021_to_2024_with weather.csv` 在项目根目录下。

数据包含：
- datetime: 时间戳
- Power demand: 电力负荷（MW）
- temp, dwpt, rhum, wdir, wspd, pres: 气象数据
- year, month, day, hour, minute: 时间特征

## 使用方法

### 1. 启动系统

**Windows系统**：
双击运行 `start_server.bat`

**Linux/MacOS系统**：
```bash
cd server
python manage.py runserver 127.0.0.1:8000
```

### 2. 访问Web界面
打开浏览器访问：
```
http://127.0.0.1:8000/dashboard/
```

### 3. 运行预测流程
1. 点击"启动预测流程"按钮
2. 系统将自动完成数据预处理、模型训练、预测计算、异常识别
3. 查看预测结果图表、统计数据和异常识别表格

### 4. 生成智能简报
1. 预测完成后，点击"生成简报"按钮
2. 系统将调用百度千帆大模型生成专业分析简报
3. 简报包含关键结论、风险提示、运行建议三个模块

## API接口文档

系统提供以下REST API接口：

### 1. 运行预测
```
POST /api/run_prediction/
```

返回：
```json
{
  "status": "success",
  "message": "预测完成",
  "stats": {...},
  "anomaly_count": 5
}
```

### 2. 获取预测结果
```
GET /api/get_prediction_results/
```

返回完整预测数据、图表、异常列表。

### 3. 生成分析简报
```
POST /api/generate_report/
```

调用百度千帆大模型生成简报。

## 模型参数配置

在 `myModel.lstm.py` 中可调整以下参数：

```python
LOOK_BACK = 288 * 2  # 回顾天数（默认2天）
FORECAST_HORIZON = 288  # 预测时长（默认24小时）
BATCH_SIZE = 64  # 训练批次大小
EPOCHS = 20  # 训练轮数
LR = 0.001  # 学习率
```

## 异常检测阈值配置

可调整以下阈值参数：

- Z-Score阈值：3.5（控制异常值剔除）
- 差分阈值：99.5分位数（控制突增/突降识别）
- 历史偏离阈值：20%（控制持续偏高/偏低识别）

## 输出文件

系统运行后将生成以下文件：

- `prediction_results.json`: 预测结果数据
- `forecast_report.md`: 智能分析简报

## 性能优化建议

1. **数据规模**: 建议使用2021-2023年数据进行训练，2024年数据用于测试
2. **GPU加速**: 安装CUDA版本的PyTorch可大幅提升训练速度
3. **模型缓存**: 可保存训练好的模型，避免重复训练
4. **异步处理**: 使用Celery实现异步预测，提升用户体验

## 故障排查

### 常见问题

**1. 数据文件找不到**
```
FileNotFoundError: 未找到数据文件
```
解决方案：检查数据文件路径是否正确

**2. 百度千帆API调用失败**
```
API 调用失败
```
解决方案：检查API密钥配置是否正确

**3. 模型训练时间过长**
```
训练时间超过预期
```
解决方案：减少EPOCHS参数或使用GPU加速

**4. Django服务器无法启动**
```
端口被占用
```
解决方案：修改启动端口或关闭占用进程

## 系统扩展

### 1. 添加新数据源
在 `myModel.lstm.py` 中修改 `file_path` 参数

### 2. 集成其他预测模型
可扩展支持ARIMA、Prophet、Transformer等模型

### 3. 添加更多气象特征
在特征工程部分添加更多气象数据

### 4. 实现72小时预测
修改 `FORECAST_HORIZON = 288 * 3`

## 版权信息

本项目基于开源协议发布，仅供学习与研究使用。

## 技术支持

如有问题或建议，请通过以下方式联系：
- 项目GitHub Issues
- 邮件联系开发团队

---

**更新时间**: 2026-06-28
**版本**: v1.0.0